# HomeCore DEV configuration
#
# Used when running HomeCore from the development workspace.
# Launch with run-dev.sh (repo root) or:
#   homecore --home /home/john/RustroverProjects/homeCore/homeCore \
#            --config config/homecore.dev.toml
#
# Plugin binary paths are relative to HOMECORE_HOME, which in development is
# this repo. Plugins are workspace members now, so `cargo build` puts every one
# of them in this repo's target/debug/ and no install step is needed.
#
# DO NOT use this config in production — use config/homecore.toml instead.

# ---------------------------------------------------------------------------
# Server
# ---------------------------------------------------------------------------
[server]
host = "0.0.0.0"
port = 8080

# ---------------------------------------------------------------------------
# Embedded MQTT broker
# ---------------------------------------------------------------------------
[broker]
host = "127.0.0.1"
port = 1883
# v5_port = 1884

# ---------------------------------------------------------------------------
# Location
# ---------------------------------------------------------------------------
[location]
latitude  = 38.9072
longitude = -77.0369
timezone  = "America/New_York"

# ---------------------------------------------------------------------------
# Battery alerts
# ---------------------------------------------------------------------------
[battery]
threshold_pct    = 20.0
recover_band_pct = 5.0
# notify_channel       = "all"
# notify_on_recovered  = false

# ---------------------------------------------------------------------------
# Prometheus metrics endpoint  (GET /api/v1/metrics)
# ---------------------------------------------------------------------------
# IP whitelist — default-deny. Empty list = endpoint returns 403 to everyone.
[metrics]
whitelist = [
    "127.0.0.1",
    "10.0.10.200",
]

# ---------------------------------------------------------------------------
# Authentication
# ---------------------------------------------------------------------------
[auth]
# jwt_secret is auto-generated and persisted to {state_db parent}/jwt_secret
# (0600). Do not set inline — it would commit the signing key into git.
token_expiry_hours = 24

whitelist = [
    "127.0.0.1",
    "10.0.10.200",
]

# ---------------------------------------------------------------------------
# Storage  (defaults to {base_dir}/data/ — fine for dev)
# ---------------------------------------------------------------------------
[storage]
# state_db_path   = "data/state.redb"
# history_db_path = "data/history.db"

# ---------------------------------------------------------------------------
# Rules  (defaults to {base_dir}/rules/)
# ---------------------------------------------------------------------------
[rules]
# dir = "rules"

# ---------------------------------------------------------------------------
# Ecosystem profiles  (defaults to {base_dir}/config/profiles/)
# ---------------------------------------------------------------------------
[profiles]
# dir = "config/profiles"

# ---------------------------------------------------------------------------
# Logging  — pretty stderr only in dev; file logging off to reduce noise
# ---------------------------------------------------------------------------
[logging]
level = "debug"
time_display = "local"   # "local" (default) | "utc"

[logging.targets]
# hc_core        = "debug"
# hc_mqtt_client = "debug"
# hc_broker      = "warn"

# Live log streaming over WebSocket (GET /api/v1/logs/stream).
[logging.stream]
enabled          = true
ring_buffer_size = 500

[logging.stderr]
enabled = false
format  = "pretty"
ansi    = true

[logging.file]
enabled          = true
prefix           = "homecore"
rotation         = "daily"
max_size_mb      = 100
compress         = true
prune_after_days = 5
format           = "pretty"
#format          = "json"

# Dedicated rule/automation log — captures only hc_core at debug level.
# Clean audit trail: every trigger check, condition result, and action step.
[logging.rules_file]
enabled          = true
prefix           = "rules"
rotation         = "daily"
max_size_mb      = 100
compress         = true
prune_after_days = 5
format           = "pretty"

[logging.syslog]
enabled = false
transport = "udp"
host    = "192.168.1.100"
port    = 514
protocol = "rfc5424"
facility = "daemon"
app_name = "homecore"

# ---------------------------------------------------------------------------
# Startup
# ---------------------------------------------------------------------------
[startup]
plugin_ready_delay_secs = 10

[shutdown]
drain_timeout_secs = 10

[web_admin]
enabled = true

[scheduler]
catchup_window_minutes = 15

# ---------------------------------------------------------------------------
# Plugins — paths relative to HOMECORE_HOME, i.e. this repo.
# Each plugin is a workspace member under plugins/; its binary lands in the
# shared target/debug/. config.dev.toml is gitignored and holds real
# credentials, so it exists only on a developer's machine.
# ---------------------------------------------------------------------------

[[plugins]]
id      = "plugin.yolink"
binary  = "target/debug/hc-yolink"
config  = "plugins/hc-yolink/config/config.dev.toml"
enabled = true

[[plugins]]
id      = "plugin.lutron"
binary  = "target/debug/hc-lutron"
config  = "plugins/hc-lutron/config/config.dev.toml"
enabled = true

[[plugins]]
id      = "plugin.caseta"
binary  = "target/debug/hc-caseta"
config  = "plugins/hc-caseta/config/config.dev.toml"
enabled = true

[[plugins]]
id      = "plugin.sonos"
binary  = "target/debug/hc-sonos"
config  = "plugins/hc-sonos/config/config.dev.toml"
enabled = true

[[plugins]]
id      = "plugin.zwave"
binary  = "target/debug/hc-zwave"
config  = "plugins/hc-zwave/config/config.dev.toml"
enabled = true

[[plugins]]
id      = "plugin.hue"
binary  = "target/debug/hc-hue"
config  = "plugins/hc-hue/config/config.dev.toml"
enabled = true

[[plugins]]
id      = "plugin.wled"
binary  = "target/debug/hc-wled"
config  = "plugins/hc-wled/config/config.dev.toml"
enabled = true

[[plugins]]
id      = "plugin.roku"
binary  = "target/debug/hc-roku"
config  = "plugins/hc-roku/config/config.dev.toml"
enabled = true

[[plugins]]
id      = "plugin.isy"
binary  = "target/debug/hc-isy"
config  = "plugins/hc-isy/config/config.dev.toml"
enabled = false

[[plugins]]
id      = "plugin.thermostat"
binary  = "target/debug/hc-thermostat"
config  = "plugins/hc-thermostat/config/config.dev.toml"
enabled = true

# hc-matter was never written — the repo was an empty stub and was archived
# rather than folded in, so neither path below exists. Kept, disabled, as the
# placeholder it always was; see docs/matterPlan.md.
[[plugins]]
id      = "plugin.matter"
binary  = "target/debug/hc-matter"
config  = "plugins/hc-matter/config/config.dev.toml"
enabled = false

[[plugins]]
id      = "plugin.ecowitt"
binary  = "target/debug/hc-ecowitt"
config  = "plugins/hc-ecowitt/config/config.dev.toml"
enabled = true

# hc-captest — conformance plugin for the capability/streaming contract.
# Exposes 6 demo streaming actions with no real-device dependency; used
# both by core/tests/streaming_test.rs and manual browser smoke tests
# of the Leptos ActionDrawer.
[[plugins]]
id      = "hc-captest"
binary  = "target/debug/hc-captest"
config  = "plugins/hc-captest/config/config.dev.toml"
enabled = true

# ---------------------------------------------------------------------------
# Notifications
# ---------------------------------------------------------------------------
# Uncomment and set real values to enable. Leaving "CHANGE_ME" in place
# now causes the server to refuse to start (S4 in release_0_1_1.md).
# [[notify.channels]]
# name      = "telegram"
# type      = "telegram"
# bot_token = "123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11"
# chat_id   = "-100123456789"
