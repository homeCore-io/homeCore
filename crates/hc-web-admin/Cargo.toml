[package]
name = "hc-web-admin"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
axum       = { workspace = true }
tower-http = { version = "0.6", features = ["fs"] }
tracing    = { workspace = true }
