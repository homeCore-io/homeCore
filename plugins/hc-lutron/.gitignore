debug/
target/
**/*.rs.bk
*.pdb
**/mutants.out*/
.idea/

# Local config contains real credentials — never commit
# Copy config/config.toml.example → config/config.toml and fill in your values.
config/config.toml
config/config.dev.toml
logs/
